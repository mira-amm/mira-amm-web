# Mock Development Environment
# Use mock indexer instead of real Subsquid endpoint
NEXT_PUBLIC_USE_MOCK_INDEXER=true

# Optional: Override other settings for development
NEXT_PUBLIC_ENVIRONMENT=development-mock

# You can still set a mock endpoint for testing different configurations  
# NEXT_PUBLIC_SUBSQUID_ENDPOINT=https://mock-indexer-dev.local