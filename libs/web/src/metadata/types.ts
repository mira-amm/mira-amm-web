export interface BrandMetadata {
  brandName: string;
  siteName: string;
  baseUrl: string;
  defaultTitle: string;
  defaultDescription: string;
  defaultImage: string;
  favicon: string;
}

export type BrandType = "mira" | "microchain";
