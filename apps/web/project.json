{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project web --web",
  "targets": {
    "dev": {
      "// comment": "Run web app without any dependencies. Start indexer services separately."
    },
    "dev:local": {
      "dependsOn": ["indexer:db:up"],
      "// comment": "Run web app with local NX development (starts dev database automatically)"
    },
    "test": {
      "dependsOn": ["build"],
      "command": "vitest",
      "options": {
        "cwd": "apps/web"
      }
    }
  }
}
