---
title: Installation
icon: SquareTerminal
---

import {Accordion, Accordions} from "fumadocs-ui/components/accordion";

<div className='fd-steps [&_h2]:fd-step'>

## Set up codebase
<Accordions>
  <Accordion title="Experimental Installer">
```sh title="Shell"
curl -fsSL https://raw.githubusercontent.com/mira-amm/mira-amm-web/main/install | sh
```
</Accordion>
</Accordions>

Fork and Clone [Repository](https://github.com/mira-amm/mira-amm-web).


<Tabs groupId="clone-repository" items={['Git', 'GitHub CLI']} persist>
  <Tab value="Git">
```sh title="Shell"
git clone https://mira-amm/mira-amm-web; # clone repo
cd mira-amm-web;                         # Navigate into directory
./install                                # run install script
  ```
  </Tab>
  <Tab value="GitHub CLI">

```sh title="Shell"
gh repo fork mira-amm/mira-amm-web --clone; # fork and clone repo
cd mira-amm-web;                            # Navigate into directory
./install                                   # run install script
```
  </Tab>
  </Tabs>

```sh title="Terminal"
pnpm exec playwright install --with-deps # install Playwright Browsers for E2E Testing
```

Open editor:

<Tabs groupId="open-editor" items={['VS Code', 'Neovim', 'Emacs']} persist>
  <Tab value="VS Code">
   ```sh title="Shell"
    # allow all hosts and trust the authors when you see a popup
    # and install all recommended extensions
   code .
   ```
</Tab>
  <Tab value="Neovim">
   ```sh title="Shell"
   nvim .
   ```
  </Tab>
  <Tab value="Emacs">
   ```sh title="Shell"
   emacs .
   ```
  </Tab>
</Tabs>

  ## Run projects

⭐ Enjoy Microvisor, our project-specific IDE.

<Tabs groupId="project-commands" items={['Terminal Mode', 'Browser Mode']} persist>
  <Tab value="Terminal Mode">
   ```sh title="Shell"
   pnpm dev
   ```
</Tab>
  <Tab value="Browser Mode">
   ```sh title="Shell"
   pnpm start
   ```
  </Tab>
</Tabs>

- Move panes & tabs: <kbd>Ctrl</kbd> + <kbd>left/right/up/down</kbd>
- Toggle popup panes <kbd>Ctrl</kbd> + <kbd>F</kbd>

</div>
