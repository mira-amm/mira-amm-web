{
  "name": "design",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/design",
  "projectType": "application",
  "targets": {
    "build": {
      "dependsOn": [
        "ts-sdk:build"
      ],
      "command": "nx build-storybook",
      "options": {
        "cwd": "apps/design"
      }
    }
  }
}
